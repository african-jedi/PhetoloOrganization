apiVersion: v1
kind: PersistentVolume
metadata:
  name: math28-postgresql-pvc
  namespace: math28
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 5Gi
  hostPath:
    path: /data/pv0001/ # must exist inside Minikube